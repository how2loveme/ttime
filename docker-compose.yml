version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./db:/app/db
      - ./staticfiles:/app/staticfiles
    environment:
      - DEBUG=0
      - SECRET_KEY=change-me-in-production
    restart: unless-stopped
