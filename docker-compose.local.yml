version: '3.9'
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: compose_vote
      POSTGRES_USER: vote_user
      POSTGRES_PASSWORD: vote_password
    ports:
      - "5432:5432"